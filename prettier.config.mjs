/** @type {import("prettier").Config} */
const config = {
  trailingComma: 'all',
  tabWidth: 2,
  semi: false,
  singleQuote: true,
  arrowParens: 'always',
  printWidth: 100,
  useTabs: false,
  quoteProps: 'consistent',
  bracketSameLine: false,
}

export default config
